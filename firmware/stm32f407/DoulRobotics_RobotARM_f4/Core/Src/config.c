//###########################################################################
// FILE:    config.c
// TITLE:   튜닝/설정 파라미터 기본값 정의
//
// 부팅 시 g_axis_cfg가 이 값으로 시작.
// 런타임에 UART GAIN 명령 등으로 덮어쓸 수 있음.
//###########################################################################

#include "config.h"

/* =========================================================
 * 축별 기본 설정
 *  [0]=J0  [1]=J1  [2]=J2
 *  게인은 부하에 맞게: 무거운 관절일수록 Kp 크게
 *  각도제한은 기구부 가동범위에 맞게 조정
 * ========================================================= */
AxisConfig_t g_axis_cfg[MOTOR_COUNT] = {
    /* J0 */ { .kp = 50.0f, .kd = 1.5f, .q_min = -3.14f, .q_max = 3.14f },
    /* J1 */ { .kp = 40.0f, .kd = 1.2f, .q_min = -1.57f, .q_max = 1.57f },
    /* J2 */ { .kp = 30.0f, .kd = 1.0f, .q_min = -2.00f, .q_max = 2.00f },
};

/* =========================================================
 * 초기화
 *  - 현재는 g_axis_cfg가 정적 초기화되므로 별도 작업 없음
 *  - 추후 EEPROM/Flash에서 설정 로드 시 여기서 처리
 * ========================================================= */
void config_Init(void)
{
    /* 정적 초기값 사용. 필요 시 여기서 재설정 */
}